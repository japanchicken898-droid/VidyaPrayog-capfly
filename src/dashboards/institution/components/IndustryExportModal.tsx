/**
 * IndustryExportModal
 * Backward-compatible re-export. All Industry components can continue using
 * `import { IndustryExportModal } from './IndustryExportModal'` without changes.
 * The actual implementation now lives in InstitutionExportModal.tsx (portal-wide).
 */
export { InstitutionExportModal as IndustryExportModal } from './InstitutionExportModal';
export type { InstitutionExportConfig as ExportConfig } from './InstitutionExportModal';
